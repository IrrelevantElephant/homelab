apiVersion: apps/v1
kind: Deployment
metadata:
  name: otel-rabbitreceiver
  labels:
    app: otel-rabbitreceiver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: otel-rabbitreceiver
  template:
    metadata:
      labels:
        app: otel-rabbitreceiver
    spec:
      serviceAccountName: otel-rabbitreceiver
      containers:
        - name: otel-rabbitreceiver
          image: 'rg.fr-par.scw.cloud/otel-collector/rabbitexporter:0.3.1'
          imagePullPolicy: Always
          ports:
            - containerPort: 4317 # OTLP gRPC endpoint
            - containerPort: 55680 # OTLP HTTP endpoint
          volumeMounts:
            - name: otel-rabbitreceiver-config
              mountPath: /etc/otel/config.yaml
              subPath: config.yaml
          args: 
            - "--config=/etc/otel/config.yaml"
          env: 
            - name: RABBITMQ_USERNAME
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-credentials
                  key: username
            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-credentials
                  key: password
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"
      volumes:
        - name: otel-rabbitreceiver-config
          configMap:
            name: otel-rabbitreceiver-config
      imagePullSecrets:
        - name: regcred
